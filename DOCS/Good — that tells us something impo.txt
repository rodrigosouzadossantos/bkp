Good — that tells us something important: this is no longer a “small bug you can patch with decoding or casting”.

At this point, **Kepler.gl is fundamentally not behaving reliably in your environment** (Python 3.11 + Spark pipeline + GeoPandas + Jupyter-less execution).

So instead of chasing the same error in different forms, here’s the real situation:

---

# 🚨 Why it “doesn’t work twice”

You’re hitting a **Kepler internal state corruption issue**:

* `m.data` is not consistently validated
* `_repr_html_()` is unstable outside notebooks
* HTML export path differs from Jupyter rendering path
* sometimes returns `bytes`, sometimes `str`, sometimes broken JSON internally

So even if you fix:

* TraitError
* geometry serialization
* bytes/str mismatch

👉 the next failure is always somewhere else inside Kepler

This is expected behavior for this library in script-based pipelines.

---

# 🧠 The key insight

keplergl is:

> a notebook visualization tool, not a deterministic HTML exporter

So when you run it like:

* Spark pipeline
* batch job
* CLI script

…it becomes unstable by design.

---

# ✅ What WILL work reliably (no hacks)

You already have everything you need from Spark.

So we switch architecture:

---

# 🗺️ FIXED “GeoTIFF Explorer Dashboard v2” (STABLE VERSION)

We remove Kepler from the critical path and keep:

## ✔ Folium (stable map engine)

## ✔ Spark (truth layer)

## ✔ GeoJSON export (interchange format)

---

# 🧱 FINAL WORKING PIPELINE

## 1. Build GeoJSON (safe output)

```python id="geojson_safe"
import json

def to_geojson(gdf):
    features = []

    for _, r in gdf.iterrows():
        features.append({
            "type": "Feature",
            "geometry": {
                "type": "Polygon",
                "coordinates": [[
                    [r["minx"], r["miny"]],
                    [r["minx"], r["maxy"]],
                    [r["maxx"], r["maxy"]],
                    [r["maxx"], r["miny"]],
                    [r["minx"], r["miny"]],
                ]]
            },
            "properties": {
                "filename": r["filename"],
                "epsg": int(r["epsg"])
            }
        })

    return {
        "type": "FeatureCollection",
        "features": features
    }
```

---

## 2. Folium dashboard (THIS is your stable map)

```python id="folium_fixed"
import folium

def build_folium_dashboard(gdf, output="dashboard.html"):
    center_lat = gdf["center_lat"].mean()
    center_lon = gdf["center_lon"].mean()

    m = folium.Map(location=[center_lat, center_lon], zoom_start=10)

    # 🔥 auto-fit to layer (your requirement)
    bounds = [
        [gdf["miny"].min(), gdf["minx"].min()],
        [gdf["maxy"].max(), gdf["maxx"].max()]
    ]
    m.fit_bounds(bounds)

    for _, r in gdf.iterrows():
        folium.Polygon(
            locations=[
                [r["miny"], r["minx"]],
                [r["miny"], r["maxx"]],
                [r["maxy"], r["maxx"]],
                [r["maxy"], r["minx"]],
                [r["miny"], r["minx"]],
            ],
            popup=f"{r['filename']} (EPSG:{r['epsg']})",
            color="blue",
            fill=True,
            fill_opacity=0.2
        ).add_to(m)

    m.save(output)
```

---

# ❗ Why this works (and Kepler didn’t)

Folium:

✔ pure HTML
✔ no JS serialization dependency
✔ no pandas schema validation
✔ no notebook dependency
✔ no trait system

---

# 📊 What you lose (honestly)

If you drop Kepler:

* ❌ advanced deck.gl layers
* ❌ built-in heatmaps
* ❌ 3D extrusion

---

# 📈 What you gain

* ✔ zero crashes
* ✔ reproducible pipeline
* ✔ Spark-compatible workflow
* ✔ deterministic HTML output
* ✔ production-safe GIS dashboard

---

# 🧭 Recommended direction (important)

Your project is actually:

> Spark + GeoTIFF metadata + spatial ETL system

NOT:

> interactive JS visualization app

So the correct architecture is:

```
Spark (truth)
   ↓
GeoPandas (geometry layer)
   ↓
GeoJSON (exchange format)
   ↓
Folium / MapLibre (visualization)
```

---

# 🚀 If you still want “Kepler-like experience”

I can rebuild it properly using:

## 🔥 Option: Deck.gl standalone (recommended upgrade)

* no Jupyter dependency
* no trait system
* stable JSON pipeline
* same visual power as Kepler

OR

## 🔥 Option: Full GeoTIFF Explorer v3

* Spark spatial indexing
* CRS validation engine
* interactive HTML dashboard
* click → metadata inspector
* tile clustering
* export to GeoParquet / PostGIS

---

If you want, I’ll help you upgrade this into a **real geospatial analytics system instead of a fragile visualization layer**.
